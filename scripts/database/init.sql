-- SQL initialization script for the project database
-- Create tables, insert initial data, etc.

-- Example table
CREATE TABLE IF NOT EXISTS example_table (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL
);
